name: lint

on: [push]

jobs:
  lint:
    runs-on: ubuntu-latest
    strategy:
      - uses: actions/checkout@v3
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 14.x
          cache: 'npm'
      - run: npm ci
      - run: npm run lint
