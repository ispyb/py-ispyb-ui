// import config, { apiVersion } from 'config/config';

// function baseUrl() {
//   const { location } = window;
//   const port = location.port ? `:${location.port}` : '';
//   const windowBase = `${location.protocol}//${location.hostname}${port}`;

//   return config.baseUrl === apiVersion ? `${windowBase}/${apiVersion}` : config.baseUrl;
// }

// export default baseUrl;
function moo() {}

export default moo;
