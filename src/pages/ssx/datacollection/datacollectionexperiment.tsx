import { SSXDataCollectionResponse } from '../model';

export default function SSXDataCollectionExperiment({ dc }: { dc: SSXDataCollectionResponse }) {
  return <div style={{ height: 150, textAlign: 'center' }}>TODO: Experiment</div>;
}
