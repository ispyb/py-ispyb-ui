import { SSXDataCollectionResponse } from '../model';

export default function SSXDataCollectionProcessing({ dc }: { dc: SSXDataCollectionResponse }) {
  return <div style={{ height: 150, textAlign: 'center' }}>TODO: Processing</div>;
}
